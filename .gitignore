# Build products
builddir/
compile_commands.json

# Emacs lockfiles & autosaves
*~
\#*\#
.\#*

# Local debug captures
foo.txt

# stale install-prefix dir, ignore
bldInstall/
data.asm
text.asm

# exported OCI image tars (make image-export) — large, never commit
spimulator-*.tar
