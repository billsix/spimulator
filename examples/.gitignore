# Build artifacts
builddir/
compile_commands.json

# Compiler-generated native assembly listings (src/Makefile).
# Hand-written sources are all *.asm, so *.s is safe to ignore.
*.s

# Sphinx book outputs
output/
book/build/
book/version.txt

# Editor leftovers
*~
\#*\#
.\#*
.DS_Store

# Local debug captures (matching /spimulator's convention)
foo.txt
